---
trigger: always_on
---

# Git Workflow Rules

- **Explicit Push Approval Required**: ALWAYS ask the user for confirmation before executing `git push`. Do not push automatically, so the user has the opportunity to review, adjust, or add further changes first.
