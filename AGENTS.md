# Agent Guidelines & Project Rules

## Git & Deployment Workflow
- **Git Push Permission**: Always ask the user for explicit confirmation before pushing changes (`git push`) to remote repositories. Never push automatically without confirmation.
- **Commit Messages**: Write clear, descriptive commit messages summarizing the changes.
- **Testing**: Run test suite (`python3 scripts/test_app.py`) before proposing commits.
